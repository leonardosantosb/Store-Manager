// const joi = require('joi');

// const schemaProducts = joi.object({
//   name: joi.string().min(5).required(),
// });

// const validatorProducts = (product) => schemaProducts.validate(product);

// module.exports = { validatorProducts };